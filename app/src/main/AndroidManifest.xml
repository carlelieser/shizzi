<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- R1.1: required to talk to Shizuku at all. -->
    <uses-permission android:name="moe.shizuku.manager.permission.API" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Held by the shell UID at runtime, not by this app. Declared so the
      manifest records the privileged surface the UserService relies on; the
      app process is never granted these.
    -->
    <uses-permission
        android:name="android.permission.MANAGE_TEST_NETWORKS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission
        android:name="android.permission.TETHER_PRIVILEGED"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".SpikeApplication"
        android:allowBackup="false"
        android:label="Tether Spike"
        android:supportsRtl="true"
        android:theme="@style/Theme.TetherSpike">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- R1.1 / R2.3: provider is how Shizuku hands us the binder; not exported. -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="false"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

    </application>

</manifest>
